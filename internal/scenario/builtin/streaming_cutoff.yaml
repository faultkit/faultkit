name: llm-streaming-cutoff
description: Streaming chat completion drops the connection mid-token without [DONE].
experiments:
  - name: drop-after-80-tokens
    fault:
      stream_cutoff_tokens: 80
    match:
      host: api.openai.com
      path: /v1/chat/completions
    probability: 0.1
